describe('Given that an agent Already exist', () => {
  describe('When the create Landlord mutation is is Executed', () => {
    test('A LandLnd Lord should exist', () => {
      expect(true).toBe(true);
    });
  });
});
